{
  "name": "with-docker",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "build:company-users": "turbo run build --filter=company-users-service...",
    "clean": "turbo run clean",
    "dev": "dotenv -- turbo run dev",
    "dev:company-users": "dotenv -- turbo run dev --filter=company-users-service",
    "dev:admins-gateway": "dotenv -- turbo run dev --filter=admins-gateway",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "dependencies": {},
  "devDependencies": {
    "dotenv-cli": "latest",
    "@ce/eslint-config": "*",
    "prettier": "^3.1.1",
    "turbo": "latest"
  },
  "packageManager": "yarn@1.22.19",
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=7.0.0"
  }
}
