{
  "name": "@ce/logger",
  "version": "0.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "tsup --watch",
    "build": "tsup --external pino --external pino-http --external pino-pretty",
    "test": "vitest"
  },
  "devDependencies": {
    "@ce/eslint-config": "*",
    "@ce/tsup-config": "*",
    "@ce/typescript-config": "*",
    "eslint": "^8.56.0",
    "pino": "^8.18.0",
    "pino-http": "^9.0.0",
    "pino-pretty": "^10.3.1",
    "tsup": "^8.2.4",
    "typescript": "^5.3.3"
  }
}
